import expect from 'unexpected';
import localization from '../../src/javascript/common/localization';

describe('Localization', () => {
  it('should be function', () => {
    expect(localization, 'to be a', 'function');
  });
});
