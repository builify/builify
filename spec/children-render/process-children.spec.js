import expect from 'unexpected';
import proccessChildren from '../../source/javascript/children-render/process-children';

describe('Proccess Children', () => {
  it('should be function', () => {
    expect(proccessChildren, 'to be a', 'function');
  });
});
